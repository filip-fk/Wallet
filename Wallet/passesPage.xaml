<Page
    x:Class="Wallet.passesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Wallet"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{x:Null}">
        <Button Content="Load" Click="Button_Click" VerticalAlignment="Top" HorizontalAlignment="Center"/>
        <Image Name="imageView"/>
        <ListView x:Name="pass_list" Margin="0,100,0,0">
            <!--
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock Text="{Binding Path=Id, Mode=TwoWay}"/>
                        <TextBlock Text="{Binding Path=Organisation, Mode=TwoWay}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>-->
        </ListView>
    </Grid>
</Page>
